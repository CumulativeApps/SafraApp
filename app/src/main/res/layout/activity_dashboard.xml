<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/drawer_layout"
    android:fitsSystemWindows="true"
    tools:openDrawer="start"
    tools:context=".Dashboard">

    <!-- As the main content view, the view below consumes the entire
         space available using match_parent in both dimensions. -->
    <include
        android:id="@+id/layoutAppBar"
        layout="@layout/app_bar_dashboard"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <!-- The drawer is given a fixed width in dp and extends the full height of
         the container. -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="@dimen/navigation_drawer_width"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="false"
        android:background="?colorPrimary"
        app:headerLayout="@layout/nav_header"
        app:menu="@menu/navigation_drawer_menu"
        app:itemShapeInsetStart="8dp"
        app:itemShapeInsetEnd="8dp"
        app:itemIconTint="@color/nav_tint_color"
        app:itemTextColor="@color/nav_tint_color"
        app:itemTextAppearance="@style/NavigationDrawerMenuFontStyle"
        app:itemShapeAppearance="@style/NavigationShape"
        app:itemShapeFillColor="@color/nav_shape_bg_color"
        app:itemIconSize="18dp" />

</androidx.drawerlayout.widget.DrawerLayout>